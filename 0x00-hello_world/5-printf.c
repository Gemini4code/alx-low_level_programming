#include <stdio.h>

/**
 * main - function that prints a line using printf
 *
 * Return: returns 0 which indicates success
 */

int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
